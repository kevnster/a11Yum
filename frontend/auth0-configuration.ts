import { EXPO_PUBLIC_AUTH0_DOMAIN, EXPO_PUBLIC_AUTH0_CLIENT_ID } from '@env';

const config = {
  domain: EXPO_PUBLIC_AUTH0_DOMAIN,
  clientId: EXPO_PUBLIC_AUTH0_CLIENT_ID,
};

export default config;
